import styles from '@sass/components/mobile-menu.module.scss'

interface Props {

}

const NotLoggedMenu = (props: Props) => {
    return (
        <div>

        </div>
    )
}

export default NotLoggedMenu
